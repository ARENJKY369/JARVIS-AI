# JARVIS OS - Core Production Requirements
# Install with: pip install -r requirements.txt
# For full features: pip install -r requirements.txt -r requirements-dev.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0,<3.0
pydantic-settings>=2.4.0
sqlalchemy>=2.0.35
aiosqlite>=0.20.0
python-multipart>=0.0.12
python-dotenv>=1.0.1
loguru>=0.7.2
cryptography>=43.0.1
aiofiles>=24.1.0

# Core AI & Memory (local)
ollama>=0.3.3
chromadb>=0.5.5
numpy>=1.26.0

# Optional extras (install via pip install -r requirements.txt[voice] etc or use pyproject)
# See pyproject.toml for groups: [voice], [vision], [automation], [dev]
